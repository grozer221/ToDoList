export type Todo = {
    id: number,
    name: string,
    isComplete: boolean,
    deadline: string,
    dateComplete: string,
    categoryId: number,
    category: any,
    createdAt: string,
    updatedAt: string,
}